# Managed by Nix Home Manager.
#
# Source of truth:
# - nix/shared.nix (shared ZSH config)
# - nix/macos.nix (macOS overrides)
# - nix/nixos.nix (NixOS overrides)
#
# For local machine-only overrides, use ~/.zshrc.local.
