#!/usr/bin/env bash
set -euo pipefail

if [ $# -eq 0 ]; then
  echo "Usage: omarchy-launch-tui <command> [args...]" >&2
  exit 1
fi

app_id="org.omarchy.$(basename "$1")"
exec setsid foot --app-id="$app_id" -e "$@"
