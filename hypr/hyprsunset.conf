# Keep the blue-light filter active all day. Runtime changes are handled by
# hyprsunset-control.sh; disabling the filter uses Hyprsunset's identity mode.
profile {
    time = 00:00
    temperature = 4500
}
