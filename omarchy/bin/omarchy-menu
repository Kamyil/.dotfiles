#!/usr/bin/env bash
set -euo pipefail

omarchy_bin="${OMARCHY_BIN:-$HOME/.local/share/omarchy/bin}"

if [ $# -gt 0 ]; then
  exec "$omarchy_bin/omarchy-launch-walker" -q "$*"
fi

exec "$omarchy_bin/omarchy-launch-walker"
