# Error tracking

Search exception issues and releases for the target symbols, stack frames, and behavior before and after the change. Capture issue IDs, first and last seen versions, event counts, and stack traces. State when no matching error exists.
