-- Layout matching waybar configuration
-- Left: Apple icon, Workspaces (similar to waybar's omarchy + hyprland/workspaces)
-- Center: Clock (similar to waybar center)
-- Right: CPU, Memory, WiFi, Volume, Battery (similar to waybar's right modules)

require("items.apple")
require("items.spaces")
require("items.clock")
require("items.cpu")
require("items.memory")
require("items.wifi")
require("items.volume")
require("items.battery")
